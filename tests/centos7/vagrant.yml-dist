# pgver: "13"                           # PostgreSQL version to use
# pgbr_branch: "release/2.31"           # pgBackRest git branch to use for builds
# vm_prefix: "check_pgbr_c7"            # VM prefix
# encrypted_repo: "false"               # Use aes-256-cbc cipher type to encrypt the pgBackRest repository
# edb_repository_username: "username"   # EDB Repository Username
# edb_repository_password: "password"   # EDB Repository Password
